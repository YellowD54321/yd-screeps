/**
 * Screeps terrain mask constants.
 * In Jest environment these are not available globally, so we define them here.
 * @see https://docs.screeps.com/api/#Room-Terrain
 */
export const TERRAIN_MASK_WALL = 2;
